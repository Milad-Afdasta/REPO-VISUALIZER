{
  "name": "repo-visualizer",
  "version": "1.0.0",
  "description": "Defrag-style visual file activity monitor for code repositories",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "concurrently -n SERVER,CLIENT -c cyan,magenta \"node server.js\" \"npm run dev --prefix client\"",
    "build": "npm install --prefix client && npm run build --prefix client",
    "setup": "npm install && npm install --prefix client"
  },
  "dependencies": {
    "express": "^4.18.2",
    "ws": "^8.16.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
